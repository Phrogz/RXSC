* Create initial state for states without initial attribute or element.
* Data Model
* <invoke>
* Why doesn't going to action-3 via action.done kill the interpreter?
* Autogenerate IDs when missing (per specs)
* Catch Datamodel runtime errors and raise errors in the machine
* Allow single atomic <scxml> to work - probably by changing the interpreter to create a pseudo transition to the scxml state and executing that first.